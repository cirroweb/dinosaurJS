import { isOdd } from 'dinosaurjs/helpers/is-odd';
import { module, test } from 'qunit';

module('Unit | Helper | is odd');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isOdd([42]);
  assert.ok(result);
});
